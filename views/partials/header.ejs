<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-6 pt-5 pb-5 border-b border-green-100 bg-green-100 shadow-md py-3">
        <h3 class="text-xl text-green-500 font-semibold">BagShop</h3>
        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true%>
        <!--this means if the loggedin value sent is undefined then set the variable "loggedin" value as true else let the variable has the value of the loggedin sent-->
        <% if (loggedin) { %>
            <div class="flex gap-5">
                <% if (type=="user") {%> 
                    <a href="/shop">Shop</a>
                    <a href="/cart">Cart</a>
                    <a href="/users/profile">My Account</a>
                    <a class="text-red-600" href="/users/logout">Logout</a>
                    <%} else if(type=="owner") {%>
                        <!-- <a href="/shop">Shop</a>
                        <a href="/cart">Cart</a> -->
                        <a href="/owners/profile">My Account</a>
                        <a class="text-red-600" href="/owners/logout">Logout</a>
                    <%}%>
            </div>
        <% } %>
    </nav>